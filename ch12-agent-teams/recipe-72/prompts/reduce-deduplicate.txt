以下の Map 出力 (3 グループ分の JSON) を統合してください:

集約ルール:
1. file + line が完全一致する issue は 1 件にまとめる
2. category と description が類似する issue (3 行未満で要約が同じ) は
   "related_issues" 配列でグループ化する
3. severity は最大値を採用する
4. クロスグループでパターンが一致する場合 (例: 全グループで any 型混入)、
   "pattern" セクションを追加して件数とサンプル 3 件を抜粋する

最終出力は (1) サマリー (2) パターン一覧 (3) 個別 issue 全件、の
3 段構成で Markdown に整形してください。
