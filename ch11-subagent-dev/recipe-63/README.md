# レシピ63: テスト実行とレポート集約（バックグラウンド）

サブエージェントをバックグラウンドで実行し、テスト結果を待ちながら他の作業を並行して進めます。テスト実行専用のカスタムサブエージェントを定義し、レポートをファイルに出力します。

## ファイル一覧

| ファイル | 説明 |
|---------|------|
| `.claude/agents/test-runner.md` | テスト実行とレポート生成を行うカスタムサブエージェント定義 |
| `.claude/settings.json` | SubagentStop Hook によるテスト完了通知設定 |
| `prompts/background-test.txt` | バックグラウンドでテストスイートを実行するプロンプト例 |
| `prompts/parallel-test-suites.txt` | 複数テストスイートを並列バックグラウンドで実行するプロンプト例 |

## 使い方

```bash
# テスト実行エージェントをプロジェクトに配置
cp -r .claude /path/to/your-project/

# バックグラウンドでテストを実行
> バックグラウンドでテストスイートを実行し、レポートを作成してください

# 実行中にCtrl+Bでバックグラウンドに切り替えることも可能
# テスト完了後に結果を確認
> 先ほどのテスト結果を確認して。失敗テストがあれば修正して
```

## バックグラウンド実行の 3 つの方法

- **事前指定（自然言語）**: 「バックグラウンドで実行してください」と明示的に指示する
- **frontmatter 固定**: サブエージェント定義に `background: true` を加える
- **実行中切替**: サブエージェント実行中に `Ctrl+B` を押してバックグラウンドに切り替える

## バックグラウンドサブエージェントの制約

- 起動前に必要なツール許可を事前に取得する（事前承認モデル）
- 実行中は事前許可済みの操作のみ実行可能、それ以外は自動拒否
- AskUserQuestion によるユーザーへの質問はツール呼び出しが失敗する（サブエージェント自体は継続）
- 権限不足で失敗した場合、同じタスクをフォアグラウンドで再依頼すれば対話的に許可可能

## 設計のポイント

- **結果をファイルに書き出す**: `test-report.md` 等に出力することで、完了後にファイルを読むだけで結果を確認できる
- **対話不要な処理に限定する**: ユニットテスト、リンター、静的解析がバックグラウンドに最適
- **`CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`**: チームのポリシーでバックグラウンド実行を禁止する場合に設定する
- **SubagentStop Hook との連携**: テスト完了時にデスクトップ通知を送るワークフローを構築できる（matcher にエージェント名を指定）

## 関連レシピ

- レシピ60「並列実行で大規模コードベースを高速調査」
- レシピ62「プロンプトチェーンでワークフロー制御」
- レシピ85「Notification通知 + sessionTitle」
- レシピ88「Stop Hooks」
- レシピ91「CI/CDパイプライン + /autofix-pr + PR auto-fix Web」
