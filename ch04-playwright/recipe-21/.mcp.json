{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--caps", "storage",
        "--storage-state", "playwright/.auth/user.json"
      ]
    }
  }
}
