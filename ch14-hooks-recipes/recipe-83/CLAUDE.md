## 機密情報の取り扱い

- `.env` / `credentials.json` / `*.pem` / `*.key` の編集は禁止です。値を更新する必要があるときは、ユーザーに手動更新を依頼してください
- ソースコード・コメント・git commit メッセージに API キー・パスワード・トークンを直接書かないでください
- デバッグ時に環境変数を表示する必要があるときは、最初の 4 文字 + 末尾長さのフィンガープリント形式に留めてください
- 例: `echo "API_KEY: ${API_KEY:0:4}... (len=${#API_KEY})"`
